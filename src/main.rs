use rust_study::basic::index::{hs_index, hs_closures, hs_closures1, hs_thread, hs_thread1, hs_thread2, hs_thread3, hs_thread4, hs_thread5, hs_thread6};

fn main() {
    // hs_index();
    // hs_thread();
    // hs_thread1();
    // hs_thread2();
    hs_thread6();
    // hs_closures();
    // hs_closures1();
}

